import { Color } from "shared-lib/dist/styles/variables";
import styled from "styled-components";

export const StyledSection = styled.div`
  border: 1px solid ${Color.Light3};
  border-radius: 6px;
  padding: 16px;
`;
